[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ambient-will"
version = "0.2.0"
description = "Offline desire ledger and shadow simulator for proactive agents"
readme = "README.md"
requires-python = ">=3.11"
authors = []
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8", "ruff>=0.9"]

[project.scripts]
ambientwill = "ambientwill.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"
